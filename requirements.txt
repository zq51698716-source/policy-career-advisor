fastapi==0.115.0
uvicorn[standard]==0.30.0
anthropic==0.42.0
tavily-python==0.5.0
python-dotenv==1.0.1
pydantic==2.9.0
httpx==0.27.0

# RAG — enterprise-grade document understanding
pymupdf>=1.24.0              # PDF parsing (PyMuPDF, replaces PyPDF2)
chromadb>=0.5.0              # Vector database
sentence-transformers>=3.0.0 # Embedding models (BGE, M3E, etc.)
langchain-text-splitters>=0.3.0  # Semantic text chunking
rank-bm25>=0.2.2             # BM25 keyword search for hybrid retrieval

# Development & testing
pytest>=8.0
pytest-asyncio>=0.24.0
